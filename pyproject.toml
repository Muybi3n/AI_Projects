[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "inboxguard-core"
version = "0.1.0"
description = "Local-first email triage, 5-tier classification, newsletter cleaner, and security filter engine."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "bi3n", email = "Muybi3n@users.noreply.github.com" }]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0.0", "pytest-cov>=5.0.0", "ruff>=0.4.0", "build>=1.0.0"]

[project.scripts]
inboxguard = "inboxguard.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "C4", "UP", "BLE"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --strict-markers"
