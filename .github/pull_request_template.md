## Description
Summary of the changes introduced by this pull request.

## Checklist
- [ ] Tests added/updated and passing (`uv run --with pytest --with pytest-cov pytest -v`)
- [ ] Code formatted and linted with Ruff (`uv run --with ruff ruff check --fix src tests && uv run --with ruff ruff format src tests`)
- [ ] No hardcoded secrets or PII introduced
- [ ] Documentation / README updated if relevant
